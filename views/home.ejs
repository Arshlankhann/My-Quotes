<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Quotes</title>
    <link rel="stylesheet" href="home.css">
</head>

<body>
    <h1>
        Latest Quotes
        <form method="get" action="/profile"><button>My Profile</button></form>
        <form method="get" action="/logout"><button>Logout</button></form>
        
    </h1>
    <div class="container">

    </div>

    <div class="mainContainer">
        <%for(let post of posts){%>
            <div class="allpost">
                <h4>
                    <img src="/images/uploads/<%=post.profilepic%>">
                    <%=post.user.username%>
                </h4>
                <br>
                <p>
                    <%=post.content%>
                </p>
                <small>
                    <%= post.likes.length%> Likes
                </small>
                <a id="likeBtn" href="/homelike/<%=post._id%>">
                    <%= post.likes.indexOf(post._id)===-1 ? "Like" :"Unlike"%>
                </a>
                
            </div>
            <%}%>
    </div>
</body>

</html>